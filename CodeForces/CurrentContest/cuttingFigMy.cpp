#include "bits/stdc++.h"

/**
 * Understanding the algorithm
 * 
 *
 */

int solve(vector<string> vs){
    int n;
    vector<string> vs;
    
}

int main(){
    int t = 1;
    while(t--){
        int n,m;
        string s;
        vector<string> vs;
        cin >> n >> m;
        cin >> s;
        vs.push_back(s);
        cout << solve(vs) << "\n";
    }
}
