/*Exercise 1-14*/
#include <stdio.h>

#define IN 1 
#define OUT 0

main(){
    
    /*Find a way to make an array and update it with newly found chars
    make a parallel array with the same length for processing the counts
    int difCharCount[len]; char difChar[len]*/

    int c, nc, state;
    
    nc = 0;

    while(!(c=getchar())!=EOF){
        if(c==)
    }
}