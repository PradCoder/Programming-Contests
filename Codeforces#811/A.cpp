#include "bits/stdc++.h"
 
using namespace std;
 
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  
  int t; 
  cin >> t;
  while (t--) {
    int h, m;
    cin >> n >> h >> m;
    vector<pair<int,int>> pairs;
    for(int i = 0; i < n; i++){
        int hi, mi;
        cin >> hi >> mi;
        pairs.push_back({hi, mi});
    }
  }
  
}
