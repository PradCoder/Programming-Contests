/*
ID: 2010pes1
TASK: gift1
LANG: C++
*/
#include "bits/stdc++.h"


using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    ofstream fout("crypt1.out");
    ifstream fin("crypt1.in");    

    int n;
    fin >> n;
    vector<int> vi = vector<int>(n,0);
    for (int i = 0; i < n; i++) {
        fin >> v[i];
    }
    return 0;
}
