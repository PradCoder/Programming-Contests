#include "bits/stdc++.h"

using namespace std;

int solve(){

    return m;
}

int main(){
    int a, b;
    string s1, s2;
    cin >> t;
    while(t--){
        cin >> a >> b;
        cout << ((state) ? "YES" : solve() ) << "\n";
    }
    return 0;
}
