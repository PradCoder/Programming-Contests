#include "bits/stdc++.h"
 
using namespace std;
 
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  
  int t, n; 
  cin >> t;
  while (t--) {
    cin >> n >> m >> k;
    vector<int> a(k,0);
    for (int i = 0; i < k; i++){
        cout << i << " ";
    } 
    cout << "\n";
  }
  
}
