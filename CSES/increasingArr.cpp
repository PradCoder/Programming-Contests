#include "bits/stdc++.h"
#include <vector>

using namespace std;

int solve(vector<int> v){
    return 0;
}

int main(){
    int n;
    vector<int> vec;
    cin >> n;
    vec = vector<int>(n,0);
    cout << solve(vec);
    return 0;
}
