#include "bits/stdc++.h"

int main(){
    cin >> n;
    vector<pair<int,int>> pa;
    vector<pair<int,int>> pa = vector<pair<int,int>>(0,n);
    for(int i = 0; i < n; i++){
        cin >> pa[i].first >> pa[i].second;
    }

    return 0;
}
