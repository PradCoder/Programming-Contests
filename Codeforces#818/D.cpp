#include "bits/stdc++.h"

using namespace std;

int main(){
    int t, n;
    string s;
    cin >> t;
    while(t--){
        cin >> n >> s;
        vector<long long> vec;
        int mVal = 0;
        for(int i = 0; i < n; i++){
            if(s[i] == 'L'){
                
            }else if(s[i] == 'R'){
                
            }
        }
        cout << ((a == 5 && s == "Timru") ? "YES" : "NO") << "\n";
    }
    return 0;
}
