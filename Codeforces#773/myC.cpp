#include "bits/stdc++.h"
#include <cstdint>
using namespace std;

/*
 * 
 *
 * */
int solve(int64_t x, vector<int64_t> aux){
    sort(aux.begin(),aux.end());
    vector<bool> vbol;
    int i = 0, j = 0, ans = 0;
    for(int i = 0; i < v.size(); i++){
        if( v[i]*x == v[i] ){
        
        } 
    }
    return inVec;
}

int main(){
    int t;
    cin >> t;
    while(t--){
        int n;
        int64_t x;
        cin >> n >> x;
        vector<int64_t> aux(n);
        for(auto& it : aux){
            cin >> it;
        }
        cout << solve(x,aux) << "\n";
    }
    return 0;
}
