#include <iostream>
#include <vector>
#include <math.h>

using namespace std;

int main(){
    int n;
    cin >> n;
    vector<int> v = vector<int> v(n,0);
    while(n--){
        cin >> v[i];
    }
    for(int i = 0; i < v.size(); i++){
    
    }
	return 0;	
}
