#include <iostream>
#include <vector>
#include <math.h>

using namespace std;

int main(){
    int n,k,l,c,d,p,nl,np,ans;
    vector<int> v = vector<int>(n,0);
    n = k = l = c = d = p = nl = np = ans = 0;
    for(int i = 0; i < n; i++){
        cin >> n >> k >> l >> c >> d >> p >> nl >> np;
    }
    ans = 100;
    cout << ans;
	return 0;	
}
